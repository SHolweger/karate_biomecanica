# Dependencias del PROYECTO DE PRUEBAS AUTOMATIZADAS.
# Instalación:  pip install -r requirements-dev.txt
#
# Deliberadamente NO incluye mediapipe/opencv/customtkinter: la mayor parte de
# la suite (lógica biomecánica, reglas del sistema experto, máquina de estados
# y persistencia) corre sin cámara, sin GPU y sin entorno gráfico. Eso es lo
# que permite ejecutarla completa en integración continua en pocos segundos.
pytest>=8.0
pytest-cov>=5.0
matplotlib>=3.7   # requerido por persistence/reportes.py
numpy>=1.24       # requerido por los dobles de cámara (frames sintéticos)
